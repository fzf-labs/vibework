## 1. Implementation
- [x] 1.1 Update MCP settings UI to use Installed + CLI tabs and CLI grouping layout
- [x] 1.2 Split MCP server data into app-managed vs CLI groups using existing config sources
- [x] 1.3 Make CLI MCP cards read-only and add per-CLI config path display/open action
- [x] 1.4 Update empty states, labels, and localization strings for new UI
- [x] 1.5 Add/adjust tests or manual validation steps for MCP settings rendering and source separation
